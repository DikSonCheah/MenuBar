name: Context testing
on: workflow_dispatch


jobs:
  create_commit:
    runs-on: ubuntu-latest 
    permissions:
      issues: write 
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: check log
        run: |
          ls -la
          cd .github
      - name: check pwd
        run: pwd

