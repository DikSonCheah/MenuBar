name: Context testing
on: [push,workflow_dispatch]

  
jobs:
  create_commit:
    runs-on: ubuntu-latest 
    permissions:
      issues: write 
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: check log
        run: python test.py


